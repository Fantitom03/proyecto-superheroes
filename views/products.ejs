<!DOCTYPE html>
<html lang="es"> 
    <head>
        <meta charset="UTF-8">
        <title>Product List</title>
    </head>
    <body>
        <h1>Product List</h1>
        <ul>
            <% products.forEach(product => { %>
                <li><%= product.name%> - $<%= product.price &></li>
                 
            <% }); %>
        </ul>
    </body>

</html>